import { handleSubmit } from "../src/client/js/formHandler";

describe("Testing the submitting functionality", () => {
    test("Testing the handleSubmit() function", () => {
          expect(handleSubmit).toBeDefined();
    })});